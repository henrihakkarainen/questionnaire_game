<div class="card">
    <div class="card-header">
        <h5 class="card-title">{{this.title}}</h5>
    </div>
    <div class="card-body">
        <a class="btn btn-primary" href="/games/{{this._id}}">Play</a>
    </div>
</div>